#include "main.h"
/**
 * _abs - prints the absolute value of an interger
 * @num: num to be checked
 * Return: the absoluete value of num
 */
int _abs(int num)
{
	if (num >= 0)
	{
		return (num);
	}
	else
	{
		return (-num);
	}
}
